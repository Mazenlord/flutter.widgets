name: scrollable_positioned_list_example

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_widgets:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
